import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export const A1 = ()=> {
  return (
    <View styles={{backgroundColor:'green', flex: 1}}>
        <Text>Hello</Text>
    </View>
  );
}

